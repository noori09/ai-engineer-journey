# Day 1 - Setup Notes

- Installed Python 3.14.5
- Installed VS Code with Python extension
- Confirmed `python --version` works
- Set up GitHub repo

## Things I learned
- `python -v` is verbose mode, not version
- `python --version` or `python -V` shows version
- Python uses `#` for comments (not `//`)
- Python doesn't use semicolons or curly braces
- Indentation matters in Python